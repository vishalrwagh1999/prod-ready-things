# Pinned lint profile so results don't drift between ruff versions.
# Pycodestyle errors (E4/E7/E9), pyflakes (F), and import sorting (I).
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F", "I"]

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: devboard
  name: devboard-intern-role
rules:
- apiGroups: ["", "apps", "extensions"]
  resources: ["deployments", "services", "pods"]
  verbs: ["get", "list", "watch","delete"]
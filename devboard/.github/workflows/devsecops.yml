name: DevSecOps

on:
  push:
    branches: [mega-project]
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'ai-service/**'
      - '.github/workflows/**'
      - 'sonar-project.properties'
  pull_request:
    branches: [mega-project]
  workflow_dispatch:

jobs:
  prep:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.t.outputs.tag }}
    steps:
      - id: t
        run: echo "tag=sha-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

  code-quality:
    uses: ./.github/workflows/code-quality.yml
  secret-scanning:
    uses: ./.github/workflows/secret-scanning.yml
  dependency-checks:
    uses: ./.github/workflows/dependency-scan.yml
  docker-checks:
    uses: ./.github/workflows/docker-scans.yml
    secrets: inherit
  sonar-qube:
    uses: ./.github/workflows/sonar-scan.yml
    secrets: inherit
  code-tests:
    uses: ./.github/workflows/code-tests.yml

  docker-push:
    if: github.event_name != 'pull_request'
    needs: [prep, code-quality, code-tests, sonar-qube, docker-checks, dependency-checks, secret-scanning]
    uses: ./.github/workflows/docker-push.yml
    with:
      image_tag: ${{ needs.prep.outputs.tag }}
    secrets: inherit

  gitops-bump:
    if: github.event_name != 'pull_request'
    needs: [prep, docker-push]
    permissions:
      contents: write
    uses: ./.github/workflows/gitops-bump.yml
    with:
      image_tag: ${{ needs.prep.outputs.tag }}
